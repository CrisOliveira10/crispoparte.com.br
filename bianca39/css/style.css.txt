:root{
  --cor-primaria:#6b4b3e;
  --cor-botao-audio:#ffffff;
  --cor-icone-audio:#6b4b3e;
}

/* =================== BASE =================== */

body{
  margin:0;
  font-family:Arial, sans-serif;
  background:#eaeaea;
  scroll-behavior:smooth;
}

/* Container principal tipo "celular" */
.pagina{
  position:relative;
  width:100%;
  max-width:100vw;              /* usa largura real do aparelho */
  text-align:center;
  padding:0;
  overflow:hidden;
  margin:0 auto;
  background:#fff;
}

/* =================== IMAGENS / VIDEO =================== */

.full-img,
.full-video{
  width:100%;
  height:auto;
  display:block;
  object-fit:cover;
}

/* =================== BOTÃO AUDIO =================== */

.audio-btn{
  position:fixed;
  bottom:20px;
  right:20px;
  width:48px;
  height:48px;
  border-radius:50%;
  border:none;
  background:var(--cor-botao-audio);
  color:var(--cor-icone-audio);
  font-size:18px;
  cursor:pointer;
  z-index:9999;
  box-shadow:0 4px 15px rgba(0,0,0,0.3);
}

/* =================== CONTADOR =================== */

.contador-topo{
  position:absolute;
  top:8%;
  left:50%;
  transform:translateX(-50%);
  width:100%;
  text-align:center;
  color:#ffffff;
  z-index:50;
  pointer-events:none;
}

#countdown{
  font-size:6vw;   /* escala proporcional ao aparelho */
  font-weight:bold;
  margin-top:8px;
}

/* =================== CANVA =================== */

.canva-container{
  position:absolute;
  top:45%;
  left:50%;
  transform:translate(-50%,-50%);
  width:90%;
  z-index:20;
}

.canva-container iframe{
  width:100%;
  height:55vw;   /* proporcional */
  max-height:450px;
  border:none;
  border-radius:8px;
}

/* =================== BOTÕES =================== */

.btn-principal{
  padding:14px 30px;
  background:var(--cor-primaria);
  color:#fff;
  border:none;
  cursor:pointer;
  font-size:4vw;
  border-radius:6px;
}

.btn-agenda{
  position:absolute;
  bottom:8%;
  left:50%;
  transform:translateX(-50%);
  z-index:20;
}

.btn-confirmar{
  position:absolute;
  bottom:35%;
  left:50%;
  transform:translateX(-50%);
  z-index:20;
}

.btn-maps{
  position:absolute;
  bottom:20%;
  left:50%;
  transform:translateX(-50%);
  z-index:20;
  text-decoration:none;
}

/* =================== PIX =================== */

.pix-area{
  position:absolute;
  top:55%;
  left:50%;
  transform:translate(-50%,-50%);
  text-align:center;
  z-index:20;
  color:#ffffff;
  width:90%;
}

.pix-line{
  display:flex;
  gap:10px;
  justify-content:center;
  align-items:center;
  margin-top:10px;
  flex-wrap:wrap;
}

#pixCode{
  font-weight:bold;
  background:rgba(255,255,255,0.25);
  padding:8px 12px;
  border-radius:6px;
  word-break:break-all;
}

.pix-msg{
  display:none;
  margin-top:8px;
  font-size:14px;
  font-weight:bold;
}

/* =================== MODAL =================== */

.modal{
  display:none;
  position:fixed;
  inset:0;
  background:rgba(0,0,0,0.7);
  z-index:99999;
  justify-content:center;
  align-items:center;
}

.modal-content{
  background:#fff;
  padding:30px;
  width:90%;
  max-width:400px;
  border-radius:8px;
  text-align:left;
}

input{
  width:100%;
  margin-top:10px;
  padding:8px;
}

.btn-marrom{
  background:var(--cor-primaria);
  color:#fff;
  padding:10px;
  border:none;
  margin-top:15px;
  cursor:pointer;
  width:100%;
  border-radius:6px;
}

/* =================== FOOTER =================== */

footer{
  text-align:center;
  padding:30px;
  background:#f5f5f5;
  font-size:14px;
}

/* =================== DESKTOP AJUSTE =================== */

@media(min-width:768px){

  .pagina{
    max-width:430px;    /* no desktop vira mockup de celular */
    margin:0 auto;
    box-shadow:0 0 30px rgba(0,0,0,0.2);
  }

  body{
    background:#eaeaea;
  }

  #countdown{
    font-size:26px;
  }

  .btn-principal{
    font-size:16px;
  }

}
